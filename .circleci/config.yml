version: 2.1
orbs:
  newman: postman/newman@0.0.2

jobs:
  build:
    executor: newman/postman-newman-docker
    steps:
      - checkout
      - newman/newman-run:
          collection: ./ReqresAPI.json
workflow:
  nightly:
    triggers: #use the triggers key to indicate a scheduled build
      - schedule:
          cron: "0 5 * * 1-6" # use cron syntax to set the schedule